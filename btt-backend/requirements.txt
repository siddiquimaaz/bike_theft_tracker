# Bike Theft Tracker backend — Python 3.12+ (see pyproject.toml requires-python).
# Install: pip install -r requirements.txt   (GeoDjango/GDAL: see docs/README_NEWMACHINE.md)
#
# NOTE: Django 6.x requires Python 3.12+. Installing these pins under an older
# interpreter does not fail loudly — pip quietly resolves backwards to whatever
# the interpreter supports, leaving you on an outdated stack. If `pip list`
# disagrees with this file, check `python --version` first.
#
# ── Core Django ────────────────────────────────────────────────────────────────
Django==6.0.8
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
psycopg[binary]==3.3.4
django-filter==26.1

# ── ML / Analytics ─────────────────────────────────────────────────────────────
rapidfuzz==3.14.5
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.1

# ── File upload / media ────────────────────────────────────────────────────────
Pillow==12.3.0
python-magic==0.4.27; platform_system!="Windows"
python-magic-bin==0.4.14; platform_system=="Windows"

# ── Notifications ──────────────────────────────────────────────────────────────
twilio==9.10.9

# ── GeoDjango — GDAL/GEOS DLL providers (Windows dev only) ────────────────────
# On Linux/macOS install gdal via: sudo apt install gdal-bin libgdal-dev
# On Windows these pip wheels bundle the native DLLs; point GDAL_LIBRARY_PATH
# and GEOS_LIBRARY_PATH in .env at the DLLs inside the installed packages.
rasterio==1.5.0; platform_system=="Windows"
shapely==2.1.2; platform_system=="Windows"

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-django==4.12.0
pytest-cov==7.1.0
factory-boy==3.3.3

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
gunicorn==26.0.0
reportlab==5.0.0
